/** @type {import('tailwindcss').Config} */
export const content = [
  './src/**/*.{html,js}',
];

export const theme = {
  extend: {
    colors: {
      'primario': '#dedede',
    }
  }
};

export const plugins = [];